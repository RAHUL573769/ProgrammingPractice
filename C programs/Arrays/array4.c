#include<stdio.h>

int main(){


    int i,size,arr[size];
  scanf("%d",&size);
  for(i=0;i<size;i++){
      scanf("%d", &arr[size]);
         if(i%2==0){
       printf("%d",arr[i]);
   }
   else{
       printf("%d",arr[i]);
   }
  }


    return 0;
}
